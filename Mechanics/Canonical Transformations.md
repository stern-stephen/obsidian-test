# Canonical Transformations

## Overview

Canonical transformations are changes of phase-space variables that preserve the Hamiltonian form of the equations of motion.

## Definition

A transformation from $(q,p)$ to $(Q,P)$ is canonical when the new variables obey Hamilton's equations with the transformed Hamiltonian.

Equivalently, canonical variables preserve the basic Poisson bracket structure:

$$
\lbrace Q_i,P_j\rbrace = \delta_{ij}
$$

with:

$$
\lbrace Q_i,Q_j\rbrace=0,\quad \lbrace P_i,P_j\rbrace=0
$$

## Intuition

Canonical transformations are the phase-space analogue of choosing better coordinates while preserving the mechanics.

They preserve the pairing between coordinates and momenta, not just the number of variables.

## Point Transformations

A point transformation changes only the configuration coordinates:

$$
Q_i=Q_i(q)
$$

If the inverse relation is $q_i=q_i(Q)$, the momenta must transform as:

$$
P_i=\sum_j p_j \frac{\partial q_j}{\partial Q_i}
$$

Equivalently:

$$
p_i=\sum_j P_j \frac{\partial Q_j}{\partial q_i}
$$

This keeps the phase-space one-form invariant:

$$
\sum_i p_i dq_i=\sum_i P_i dQ_i
$$

That is why ordinary coordinate changes in configuration space become canonical transformations only when the momenta transform with the inverse Jacobian.

## Active View

An active canonical transformation moves the phase-space point while keeping the coordinate system fixed. If $G(q,p)$ generates an infinitesimal transformation with parameter $\epsilon$, then:

$$
\delta f=\epsilon \lbrace f,G\rbrace
$$

So:

$$
\delta q_i=\epsilon \frac{\partial G}{\partial p_i},\quad \delta p_i=-\epsilon \frac{\partial G}{\partial q_i}
$$

## Why It Matters

They reveal which features of Hamiltonian mechanics are coordinate-independent and prepare the idea that transformations can be generated by conserved quantities.

## Related Concepts

- [Hamiltonian Mechanics](Hamiltonian%20Mechanics.md)
- [Phase Space](Phase%20Space.md)
- [Poisson Brackets](Poisson%20Brackets.md)

<!-- semantic-edges
{"source":"Canonical Transformations","relation":"REFORMULATES","target":"Hamiltonian Mechanics","evidence_heading":"Overview","evidence_summary":"Canonical transformations change phase-space variables while preserving the Hamiltonian form of the equations of motion.","confidence":0.95}
{"source":"Canonical Transformations","relation":"REQUIRES","target":"Poisson Bracket Structure","evidence_heading":"Definition","evidence_summary":"The note says canonical variables preserve the fundamental Poisson brackets among coordinates and momenta.","confidence":0.95}
{"source":"Point Transformations","relation":"SPECIALIZES","target":"Canonical Transformations","evidence_heading":"Point Transformations","evidence_summary":"A point transformation changes configuration coordinates and becomes canonical when the momenta transform with the inverse Jacobian.","confidence":0.9}
{"source":"Symmetry Generators","relation":"ENABLES","target":"Canonical Transformations","evidence_heading":"Active View","evidence_summary":"The active view gives the infinitesimal canonical transformation generated by a phase-space function through Poisson brackets.","confidence":0.9}
-->
